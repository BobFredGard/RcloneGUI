# Copier vers .env puis remplir les vraies valeurs (fichier .env jamais commité)
SECRET_KEY=change-moi-en-production-chaine-longue-aleatoire
# Optionnel : surcharge les chemins par défaut (relatifs au projet)
# RCLONE_CONFIG_PATH=./rclone.conf
# RCLONE_EXE_PATH=./Rclone/rclone.exe
# DATABASE_URL=sqlite:///backups.db
# JWT_EXPIRATION_HOURS=24
